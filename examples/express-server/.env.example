# Who gets paid
PAY_TO=0x...         # your EVM address (Base mainnet)
SOLANA_PAY_TO=       # your Solana address (optional — leave blank to accept EVM only)

# API key — required for mainnet networks (get yours at dashboard.stablecoin.xyz)
API_KEY=sbc-...

# How much to charge (optional — defaults shown)
# AMOUNT=1000000000000000   # 0.001 SBC on Base (18 decimals)
# SOLANA_AMOUNT=1000000     # 0.001 SBC on Solana (9 decimals)

# Testnet (optional — uncomment to use testnet instead of mainnet)
# NETWORK=base-sepolia
# SOLANA_NETWORK=solana-devnet

# Port (optional)
# PORT=4402
